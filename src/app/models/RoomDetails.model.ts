export interface RoomDetails {
  activeEvents: number;
  color: string;
  events: number;
  host: string;
  hostAvatar: string;
  hostId: number;
  joined: boolean;
  members: number;
  name: string;
  joinedId: boolean;
  notifications: boolean;
}
